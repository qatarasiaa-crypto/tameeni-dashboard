# لوحة تحكم تأميني 🚗

لوحة تحكم احترافية لاستقبال وإدارة طلبات التأمين بدلاً من استخدام بوت تيليجرام فقط.

## ✨ المميزات

- 📊 **لوحة تحكم شاملة** مع إحصائيات مفصلة
- 📱 **تصميم متجاوب** يعمل على جميع الأجهزة
- 🔍 **بحث وتصفية** متقدم للطلبات
- 📈 **رسوم بيانية تفاعلية** لتحليل البيانات
- 💾 **تخزين محلي آمن** للبيانات
- 🔔 **إشعارات فورية** عند وصول طلبات جديدة
- 📤 **تصدير البيانات** بصيغة JSON
- ⚡ **تحديث تلقائي** للبيانات

## 🚀 التركيب

### 1. رفع ملفات لوحة التحكم

قم برفع جميع ملفات المجلد `dashboard` إلى خادمك:

```
dashboard/
├── index.html          # الصفحة الرئيسية
├── styles.css          # ملف التنسيقات
├── script.js           # ملف JavaScript
├── site-integration.js # ملف التكامل مع الموقع
└── README.md           # هذا الملف
```

### 2. تعديل موقعك الحالي

#### أ) استبدال دالة الإرسال في step1

في ملف `index.html` الخاص بك، ابحث عن:

```javascript
// دالة إرسال بيانات الخطوة الأولى إلى تيليجرام
function sendToTelegramStep1(data) {
    // كود تيليجرام الحالي
}
```

واستبدلها بـ:

```javascript
// دالة إرسال بيانات الخطوة الأولى إلى لوحة التحكم
async function sendToTelegramStep1(data) {
    try {
        // إرسال إلى لوحة التحكم
        const success = await sendToDashboardStep1(data);
        
        if (success) {
            console.log("تم إرسال بيانات الخطوة الأولى إلى لوحة التحكم بنجاح");
        }
        
        // يمكن الاحتفاظ بإرسال تيليجرام كنسخة احتياطية
        // await sendToTelegramBackup(data);
        
        return { success: true };
    } catch (error) {
        console.error("خطأ في إرسال البيانات:", error);
        return { success: false };
    }
}
```

#### ب) إضافة ملف التكامل

أضف هذا السطر في `<head>` لجميع صفحات موقعك:

```html
<script src="path/to/dashboard/site-integration.js"></script>
```

#### ج) تحديث باقي الخطوات

طبق نفس المبدأ على باقي الملفات (step2, step3, إلخ):

```javascript
// في step2
async function handleStep2Submit(data) {
    await sendToDashboardStep2(data);
    // باقي الكود...
}

// في step3
async function handleStep3Submit(data) {
    await sendToDashboardStep3(data);
    // باقي الكود...
}

// وهكذا...
```

### 3. الوصول للوحة التحكم

بعد رفع الملفات، يمكنك الوصول للوحة التحكم عبر:

```
https://yourdomain.com/dashboard/
```

## 📱 كيفية الاستخدام

### للمدير/المسؤول:

1. **الوصول للوحة**: اذهب إلى رابط لوحة التحكم
2. **مراجعة الطلبات**: في قسم "طلبات التأمين"
3. **الإحصائيات**: في قسم "الإحصائيات"
4. **البحث والتصفية**: استخدم شريط البحث والفلاتر
5. **تصدير البيانات**: اضغط على "تصدير البيانات"

### للعميل:

- العميل将继续 تقديم الطلبات كما هو معتاد
- ستظهر رسالة نجاح عند إتمام كل خطوة
- لن يلاحظ العميل أي تغيير في التجربة

## 🔧 التخصيص

### تغيير التصميم

يمكنك تخصيص الألوان في ملف `styles.css`:

```css
/* تغيير لون الهيدر */
.header {
    background-color: #your-color;
}

/* تغيير ألوان الإحصائيات */
.stat-card:nth-child(1) .stat-icon {
    background: linear-gradient(135deg, #your-color1, #your-color2);
}
```

### إضافة حقول جديدة

لإضافة حقول جديدة للطلبات:

1. أضف الحقل في موقعك
2. ارسل البيانات عبر `sendToDashboardStepX`
3. ستظهر تلقائياً في لوحة التحكم

### إعداد إشعارات البريد الإلكتروني

```javascript
// في site-integration.js
const DASHBOARD_CONFIG = {
    emailNotifications: true,
    emailAddress: 'your-email@domain.com'
};
```

## 📊 بنية البيانات

البيانات تُحفظ في localStorage بالمثال التالي:

```json
{
  "id": "1640995200000",
  "timestamp": "2021-12-31T23:59:59.000Z",
  "step": 1,
  "stepName": "البيانات الأساسية",
  "userName": "أحمد محمد",
  "phoneNumber": "0501234567",
  "offerType": "new",
  "regType": "serial",
  "idNumber": "1234567890",
  "serialNumber": "ABC123",
  "userAgent": "Mozilla/5.0...",
  "pageUrl": "https://yoursite.com/step1"
}
```

## 🔒 الأمان والخصوصية

- البيانات تُحفظ محلياً في متصفح المسؤول فقط
- لا يتم إرسال البيانات لخوادم خارجية
- يمكن مسح البيانات في أي وقت
- إمكانية تصدير البيانات للنسخ الاحتياطي

## 🆘 استكشاف الأخطاء

### لا تظهر الطلبات في لوحة التحكم

1. تأكد من رفع جميع ملفات لوحة التحكم
2. تأكد من صحة مسار ملف `site-integration.js`
3. افتح وحدة تحكم المتصفح (F12) للبحث عن الأخطاء
4. تأكد من تشغيل JavaScript في المتصفح

### خطأ في تحميل البيانات

1. امسح cache المتصفح
2. تأكد من أن localStorage مفعل في المتصفح
3. جرب في متصفح آخر

### الطلبات لا تُحفظ

1. تأكد من وجود مساحة كافية في localStorage
2. تحقق من إعدادات privacy في المتصفح
3. جرب في وضع incógnito/private

## 📞 الدعم

إذا واجهت أي مشكلة أو تحتاج مساعدة:

1. راجع هذا الملف أولاً
2. افتح وحدة تحكم المتصفح (F12) للبحث عن الأخطاء
3. تأكد من تحديث جميع الملفات بشكل صحيح

## 🔄 التحديثات المستقبلية

- [ ] قاعدة بيانات خارجية
- [ ] مصادقة المستخدمين
- [ ] تصدير إلى Excel
- [ ] إشعارات البريد الإلكتروني
- [ ] تقارير PDF
- [ ] API خارجي

---

**تم تطوير لوحة التحكم بواسطة MiniMax Agent** 🤖